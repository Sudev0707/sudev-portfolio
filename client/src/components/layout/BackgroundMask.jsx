import React from "react";
import "../../App.css";
import "./BackgroundMask.css"

const BackgroundMask = () => {
  return (
    <div className="background-mask">
      {/* Empty divs used for pseudo elements (::before & ::after) */}
    </div>
  );
};

export default BackgroundMask;
